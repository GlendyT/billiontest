
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model Songs {
  id String @id @default(uuid()) @map("_id")
  name String
  artist String
  streams Float
}